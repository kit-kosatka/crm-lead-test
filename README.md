# CRM Lead Test

Тестовое задание: мини-система для добавления лидов в CRM.

## Стек

- Python 3.12
- FastAPI
- SQLAlchemy 2.0 (Async)
- SQLite
- Pydantic v2
- Jinja2
- HTML/CSS

## Запуск

### 1. Клонировать репозиторий

```bash
git clone <repository-url>
cd crm-lead-test
```

### 2. Создать виртуальное окружение

```bash
python -m venv .venv
```

### 3. Активировать окружение

macOS / Linux

```bash
source .venv/bin/activate
```

Windows

```bash
.venv\Scripts\activate
```

### 4. Установить зависимости

```bash
pip install -r requirements.txt
```

### 5. Запустить приложение

```bash
uvicorn app.main:app --reload
```

После запуска приложение будет доступно по адресу:

```
http://127.0.0.1:8000
```

---

# Реализовано

- Добавление нового лида.
- Проверка обязательных полей (имя и телефон).
- Отображение созданных лидов.
- Хранение данных в SQLite.
- Сохранение данных после обновления страницы.
- Изменение этапа сделки.

---

# Структура проекта

```
app/
├── crud.py              # Работа с базой данных
├── database.py          # Подключение к SQLite
├── dependencies.py      # Зависимости FastAPI
├── main.py              # Создание приложения
├── models.py            # SQLAlchemy модели
├── schemas.py           # Pydantic схемы
└── routers/
    └── leads.py         # HTTP-маршруты

templates/
└── index.html           # Интерфейс

static/
└── style.css            # Стили
```

---

# Где находится основная логика

### Интерфейс

```
templates/index.html
```

### Логика обработки кнопки "Сохранить"

```
app/routers/leads.py
```

### Работа с базой данных

```
app/crud.py
```

---

# Использование AI

Во время выполнения тестового задания использовался AI как инструмент ускорения разработки.

С помощью AI были подготовлены отдельные участки кода и предложены варианты реализации.

Все полученные решения были проверены, адаптированы и интегрированы вручную.

---

# Что было проверено самостоятельно

- запуск приложения;
- создание лида;
- валидация обязательных полей;
- сохранение данных в SQLite;
- отображение данных после обновления страницы;
- изменение этапа сделки;
- корректность работы маршрутов FastAPI.